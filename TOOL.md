# 안내 코스 작업실 — 설계

`studio.py` 라는 컴퓨터용 도구를 만듭니다. **코스를 쓰고, 들어보고,
로봇에 올리는** 일을 한 자리에서 합니다.

이 문서는 코드를 쓰기 전에 정한 것들입니다. 나중에 "왜 이렇게 했나" 를
되짚을 때 보세요.

---

## 무엇 때문에 만드는가

코스를 **양산**할 참입니다. 지금은 코스 하나를 만들려면 파이썬 파일을
손으로 쓰고, `guide.py` 를 돌려야 음성이 만들어지고, 업로드는 아무 표시
없이 20분이 흐릅니다.

그리고 **폰이 두뇌가 되면 파이썬을 못 읽습니다.** 코스가 파이썬 파일인
채로는 폰이 코스를 알 방법이 없습니다.

---

## 잰 값 — 설계를 정한 숫자들

| 무엇 | 값 | 어떻게 알았나 |
|---|---|---|
| 업로드 속도 | **1 MB 당 63초** (조각당 0.19초) | `audio_room.py` |
| 코스 하나 올리기 | **약 19분** (음성 3분 22초 분량) | 위 값에서 계산 |
| 로봇 저장 용량 | **37개 / 46 MB 까지는 확인** | `audio_room.py`, 한계는 못 봄 |
| 멘트 mp3 : wav | **1 : 15** (2 MB : 30 MB) | `audio/` 실측 |

**19분이 이 도구의 설계를 지배합니다.** 용량이 아니라 시간이 제약입니다.

그래서 도구는 이렇게 동작해야 합니다.

- 이미 올라간 것은 **건너뜁니다** — 그리고 건너뛴 것이 눈에 보여야 합니다
- 19분 동안 **진행이 보입니다**
- 중간에 끊겨도 **이어서** 합니다
- 시연 **전날** 올려두는 절차가 문서에 있습니다

---

## 정한 것 ①  코스의 원본은 JSON 입니다

```
courses/
  ai3f/
    course.json      ← 사람이(또는 도구가) 쓰는 것. 이것이 원본입니다
    built.json       ← 도구가 만드는 것. 실측 길이·크기·올린 상태
  ai3f_short/
    course.json
```

### 왜 파이썬이 아닌가

도구에서 코스를 **고치려면** 읽어들여야 합니다. 파이썬 파일을 읽어
고치고 다시 쓰는 것은 됩니다 — 처음 한 번은요. 그런데 사람이 손으로 쓴
주석과 계산식이 섞여 있으면 **왕복하면서 조용히 뭉갭니다.** 이 저장소는
주석이 코드보다 깁니다. 그걸 잃으면 안 됩니다.

그리고 **폰은 파이썬을 못 읽습니다.** 어차피 한 번은 데이터로 바꿔야
합니다. 두 번 바꾸느니 처음부터 데이터로 둡니다.

### 그러면 지금 있는 `course_*.py` 는

**그대로 둡니다.** 자동 발견도 그대로 돕니다. 손으로 특별한 코스를 쓸
길은 남겨둡니다 — 계산이나 조건이 들어가는 코스는 파이썬이 낫습니다.

대신 `studio.py export <코스>` 로 파이썬 코스를 JSON 으로 뽑아낼 수
있게 합니다. **한 방향만** 지원합니다 (py → json). 되돌리기는 안 합니다.
왕복이 없으면 뭉갤 일도 없습니다.

### JSON 의 모양

지금 `scenario.py` 의 `Stop` / `Move` / `Line` 을 그대로 옮깁니다.
**새 개념을 만들지 않습니다** — 실행기가 이미 아는 모양이어야 합니다.

```json
{
  "id": "ai3f",
  "name": "AI응용소프트웨어과 외빈 안내",
  "subtitle": "외빈 대상 · 3층 한 바퀴",
  "route": "3층 엘리베이터 홀 → 301호 → 303호 → 304호 프로젝트실 안",
  "audience": "외빈",
  "steps": [
    {
      "kind": "stop", "id": "S1",
      "place": "3층 엘리베이터 홀", "purpose": "환영 인사 및 학과 소개",
      "door_deg": null, "face_back": true, "face": null,
      "lines": [
        {"key": "s1a_greet", "text": "안녕하십니까…",
         "gesture": "hello", "pause": 0.5, "note": null}
      ]
    },
    {
      "kind": "move", "id": "M1",
      "place": "홀 → 301호 앞", "purpose": "이동 예고 후 저속 주행",
      "key": "m1_to_301", "text": "지금부터 301호 강의실로…",
      "meters": 3.06, "turn_deg": 0.0, "turn_when": "before",
      "reposition": false, "narrow": false, "align": true
    }
  ],
  "optional": {"s4_extra": "…"},
  "clearance": {"S5": null}
}
```

`built.json` 은 도구가 씁니다. 손대지 마세요. **`audio/` 가 아니라
`courses/` 안에 두는 이유**는 아래 '아직 안 정한 것 ㄷ' 에 있습니다 —
한 줄로 말하면, 잰 값이 저장소에 남아야 하기 때문입니다.

```json
{
  "keys": {
    "s1a_greet": {"seconds": 11.7, "bytes": 1032192,
                  "text_hash": "…", "on_robot": true}
  },
  "uploaded_at": "2026-09-10T11:20:00+09:00"
}
```

`text_hash` 가 **바뀐 것만 올리는** 근거입니다. 지금 `texts.json` 이
하던 일을 코스별로 나눠 갖습니다.

---

## 정한 것 ②  멘트 이름에 코스를 붙입니다

로봇의 오디오 이름은 **평평합니다.** 폴더가 없습니다. 지금은 이렇게
섞여 있습니다.

```
s1a_greet  s2_room301  …        ← 지금 코스
SHORT_s1_greet  SHORT_m1_go  …  ← 짧은 코스 (이미 접두어를 쓰고 있습니다)
greet  follow_me  arrived  …    ← 공용 (park·carry·common 이 씁니다)
alert_blocked  …                ← 공용 안내 방송
```

코스가 늘면 `s2_room301` 이 두 코스에 다 있게 됩니다. 지금
`courses.register()` 가 그런 코스를 거부하는데, 거부가 아니라 **애초에
안 부딪히게** 해야 합니다.

```
<코스id>_<지점id>_<이름>       ai3f_s2_room301
alert_…                        공용 (코스에 안 딸림)
```

### 옮기는 값이 싸다는 것을 확인했습니다

다시 올릴 필요가 없습니다. 라이브러리에 **이름 바꾸기**가 있습니다.

```python
await hub.rename_record(uuid, new_name)     # webrtc_audiohub.py:84
```

19분이 아니라 **몇 초**입니다. `studio.py migrate` 가 이 일을 합니다.

---

## 정한 것 ③  화면은 브라우저입니다

`studio.py` 를 돌리면 로컬 서버가 뜨고 브라우저가 열립니다.
`remote.py` 가 하는 것과 같은 방식입니다.

### 왜 Tkinter 도 Qt 도 아닌가

**의존성이 하나도 안 늘어납니다.** `remote.py` 가 이미 표준 라이브러리만
으로 페이지를 띄우고 있고, 그 코드가 잘 돌고 있습니다. Qt 는 200 MB 를
더 깝니다 — 어제 "코스 도구는 whisper 와 분리해서 가볍게" 라고 정해놓고
다른 데서 200 MB 를 늘릴 이유가 없습니다.

**그리고 멘트를 들어봐야 합니다.** 브라우저는 `<audio>` 태그 하나로
mp3 를 재생합니다. 파이썬으로 소리를 내려면 라이브러리가 또 필요합니다.

**나중에 옮기기도 쉽습니다.** 폰 앱이 WebView 라, 이 화면의 일부는
그대로 폰으로 넘어갈 수 있습니다.

### 화면 셋

**ㄱ. 코스 목록**
어떤 코스가 있고, 각각 몇 지점 몇 분인지. 로봇에 올라간 것과 견준
상태(다 올라감 / 몇 개 다름 / 안 올라감).

**ㄴ. 코스 편집**
지점들이 세로로 늘어섭니다. 정차 지점은 대사 토막들, 이동 구간은
거리·회전각. 대사를 고치면 **▶ 들어보기** 가 켜집니다. 옆에 실측 길이와
"이 토막이 길다" 같은 경고가 붙습니다.

**ㄷ. 올리기**
무엇이 다른지 먼저 보여주고, 예상 시간을 말한 뒤, 누르면 올립니다.
줄마다 진행이 보입니다. 끊고 다시 눌러도 남은 것만 합니다.

---

## 하지 않을 것

- **안내를 돌리지 않습니다.** 그건 `guide.py` 와 폰 앱의 일입니다.
  도구는 멘트 하나를 로봇에 들려보는 것까지만 합니다.
- **거리를 재지 않습니다.** 거리와 각도는 복도에서 `measure.py` 로
  재는 값입니다. 도구는 받아 적고, 빠진 곳을 알려줄 뿐입니다.
- **음성 인식을 쓰지 않습니다.** whisper 도 torch 도 안 부릅니다.
  그래야 도구만 따로 묶어도 100 MB 대에 들어갑니다.

---

## wav 를 언제 지우는가

`audio/` 의 30 MB 중 28 MB 가 wav 입니다. 코스 100개면 3 GB 입니다.

그런데 **다시 만들 수 있는 것**입니다. mp3 만 있으면 `prepare_wav()` 가
언제든 만듭니다. 다만 만드는 데 코스당 몇십 초가 듭니다.

→ **쓰는 코스의 wav 는 남기고, 나머지는 지웁니다.** `studio.py tidy` 가
합니다. 자동으로는 안 합니다 — 지우는 일을 조용히 하면 안 됩니다.

---

## 만드는 순서

작은 것부터, 각각 로봇 없이 시험할 수 있게.

1. ~~**`coursefile.py`**~~ **— 됐습니다 (2026-09-10)**
   JSON 읽고 쓰기 · `Course` 왕복 · 검사. `coursefile_test.py` 36가지,
   로봇 없이 돕니다.

   · 왕복 검사를 **객체 단위**로 합니다. 사전끼리 견주면 `to_dict` 가
     통째로 빠뜨린 항목을 못 잡습니다 — 양쪽 다 없어서 같아 보이거든요.
     일부러 `turn_deg` 를 빼봤더니 사전 비교는 그냥 통과했습니다.
   · 알고 버리는 것 셋(`key=` 로 적은 정차 지점의 `Stop.key`)만
     예외로 두고, **딱 그것만 없어졌는지**를 시험이 확인합니다.
   · 저장소의 JSON 이 대본과 갈라지면 걸립니다. 실행기가 아직 `.py` 를
     읽으니 같은 것이 두 곳에 있는 셈이고, 그건 언젠가 갈라집니다.

2. ~~**`studio.py export`**~~ **— 1번이 합니다**
   `coursefile.py --save` 가 `courses/<id>/course.json` 을 씁니다.

3. ~~**`studio.py migrate`**~~ **— 됐습니다 (`rename_keys.py`)**
   로봇에서 10초. 대본 16곳 + 말없는 이름 둘까지.

4. ~~**`studio.py push`**~~ **— 됐습니다 (2026-09-10)**
   `studio.py` 의 `list` · `plan` · `push`. `studio_test.py` 26가지,
   가짜 허브로 돕니다 (로봇도 TTS 도 안 씁니다).

   · **한 개 올릴 때마다** `built.json` 을 씁니다. 마지막에 몰아 쓰면
     18분째에 끊겼을 때 처음부터 다시 해야 합니다.
   · 바뀐 것은 **문장의 지문**으로 가립니다. 파일 시각이 아니라요 —
     시각은 파일을 다시 만들기만 해도 바뀝니다.
   · 올리기 전에 **로봇에게 물어봅니다.** `built.json` 이 "올렸다" 고
     적어놨어도 로봇에 없을 수 있습니다.
   · 하나가 넘어져도 나머지는 갑니다.
   · 길이는 `built.json` 과 `audio/durations.json` **두 곳에** 적습니다.
     시간표를 짜는 `scenario.measured()` 가 후자를 읽거든요. 나중에
     `scenario` 가 `built.json` 을 보게 되면 한 곳으로 줄입니다.
5. **화면** — 목록 → 편집 → 올리기
   → 브라우저에 띄워 눈으로. `stop_test.py` 처럼 화면 검사기도 붙입니다.

1~4 까지는 **로봇 없이** 만들고 시험합니다. 로봇이 필요한 것은 3번의
마지막 확인과 4번의 실제 업로드뿐입니다.

---

## 아직 안 정한 것

**ㄱ. 지금 코스의 멘트 이름을 옮길 것인가**
옮기면 규칙이 깔끔해집니다. 이름 바꾸기가 몇 초라 값도 쌉니다. 다만
`scenario.py` 의 키들도 같이 바꿔야 하고, 그동안 잰 `durations.json` 의
이름도 따라가야 합니다. → **옮기는 쪽을 권합니다.** 코스가 하나뿐인
지금이 제일 쌉니다.

**ㄴ. ~~옛 멘트 9개를 지울 것인가~~ — 지우면 안 됩니다 (확인함)**
`greet` `follow_me` `arrived` `wait` `excuse_me` `bye` `settle`
`lift_ready` `power_ready` 를 "옛 멘트" 라고 적었다가 **찾아보니 전부
지금 쓰입니다.**

```
power_ready   park.py      전원 끄기 전 안전 자세
lift_ready    carry.py     들어 올리기 전
settle        common.py    천천히 엎드릴 때
wait          audio_check.py
greet · follow_me · arrived · bye    03·04·05 시연 스크립트
전부           config.py    PHRASES 에 정의
```

코스에 안 딸린 **공용 멘트**입니다. `alert_*` 와 같은 부류고, 오히려
접두어를 붙여 그렇게 보이게 하는 편이 낫습니다.

→ 지우지 않습니다. 코스 접두어도 안 붙입니다. 다만 목록에서 **"공용"**
  으로 묶어 보여줍니다. 도구가 코스를 비울 때 이것들을 건드리면 `park.py`
  와 `carry.py` 가 조용히 벙어리가 됩니다 — 안전에 쓰는 멘트들입니다.

**ㄷ. ~~코스 폴더를 저장소에 넣을 것인가~~ — 넣습니다, 그리고 이게 중요합니다**
`audio/` 는 이미 `.gitignore` 에 있습니다. 그 안에 `durations.json` 도
같이 들어 있고요 — **잰 값이 저장소에 안 남습니다.**

```
.gitignore:49    audio/
.gitignore:52    ※ audio/durations.json 은 audio/ 안에 있어 같이 빠집니다.
```

멘트 길이는 **한 번 재면 계속 쓰는 값**입니다. 다른 PC 로 옮기거나
`audio/` 를 비우면 다시 재야 하고, 그러려면 음성을 다시 만들어야 합니다.

→ 그래서 `built.json` 을 `audio/` 가 아니라 **`courses/<id>/` 에 둡니다.**
  거기는 추적됩니다. 음성 파일은 여전히 안 올라가지만, **잰 값은
  남습니다.** 새 PC 에서 `studio.py push` 를 돌리면 음성만 다시 만들고
  길이는 이미 알고 있습니다.
