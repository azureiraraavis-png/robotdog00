# 3층에서 할 일

한 번 다녀왔습니다. 이 문서는 이제 **끝난 것과 남은 것**을 가릅니다.
끝난 항목을 지우지 않고 값과 함께 남겨둔 이유는, 다음에 잰 값이 이것과
다르면 그 차이 자체가 신호이기 때문입니다.

---

## 끝났습니다

| 잰 것 | 값 | 어디에 들어갔나 |
|---|---|---|
| M1 대기 위치 → S2 | 3.06 m (turn −167°) | `scenario.py` M1 |
| M2 S2 → S3 | 9.66 m (turn −138°) | M2 |
| M3 S3 → S4 | 1.19 m (turn −127°) | M3 |
| M4 문 통과 | 1.07 m | M4 (`narrow=True`) |
| 문 쪽으로 도는 각도 | S2 +130° · S3 +129° · S4 −72° | `Stop.door_deg` |
| 복도 실폭 (로봇 높이) | **1.15 m** (도면 2.20 m) | `CLEARANCE` |
| 304호 문틀 유효폭 | **0.70 m** (몸통 0.31 m → 양옆 0.19 m) | M4 |
| 제자리 회전 필요폭 | 0.483 m (반경 0.383 m) | `TURN_NEED` |
| 주행거리계 폐합오차 | **4.7%** (35.45 m 에 1.66 m) | `ODOMETRY` |
| 동작 시간 | hello 4.7 · sit 7.0 · stand 7.0 · lie 6.4 초 | `GESTURE_SECONDS` |
| 호실 문패 | **301 → 303 → 304** | `SIGNS` |
| 각 정차점의 벽 | S2·S3 왼쪽(윗줄), S4 오른쪽(아래줄) | `SIGNS` |

**줄자는 한 번도 안 썼습니다.** 폐합오차 — 출발점으로 돌아왔을 때 얼마나
어긋나 있는가 — 로 대신했습니다. 4.7% 는 보수적인 상한입니다.

그리고 그 4.7% 가 결론을 하나 냈습니다.
**문틀은 거리로 못 들어갑니다.** 9.66 m 를 가면 오차가 0.45 m 인데
문틀 여유가 한쪽에 0.19 m 입니다. 두 배 넘게 모자랍니다.

---

## 남은 것 — 다음에 3층에 갈 때

- [ ] **S5 마무리 자리를 테이프로 표시하고 M5 를 잰다**
      304호 안, 로봇이 회전할 수 있는 자리로 정하셨습니다.
      지금 `M5.meters = 3.58` 은 잠정값이고 `CLEARANCE["S5"]` 는 비어 있습니다.
      → `.\run measure.py` 로 S4 → S5 한 구간만 잡으면 됩니다.

- [ ] **S1·S2·S3·S4 정차 지점도 테이프로 표시한다**
      표시가 없으면 다음 실측이 이번과 다른 값이 되고, 그러면 둘 다 못 믿습니다.

- [ ] (선택) **복도 왼쪽 벽에 문이 있는지 눈으로 확인**
      도면에는 그쪽에 문이 안 보이는데 우리가 잰 각도는 왼쪽을 봅니다.
      문패가 확정됐으므로 대사에는 지장 없습니다. 눈에 들어오면 적어두는 정도로.

---

## 주의 — 이건 그대로입니다

- 라이다는 **유리를 제대로 못 봅니다.** 복도 양쪽이 거의 전 구간 유리벽입니다.
  폭이 갑자기 넓게 나오는 지점은 유리를 통과해 본 것입니다 — 버리세요.
- 반사가 심해 **되돌아온 점의 절반 이상이 바닥 아래로 찍힙니다.** 높이로
  걸러내고 있습니다. 숫자가 이상하면 이걸 먼저 의심하세요.
- 문틀 사이에 있을 때 로봇은 **큰 회전을 거부합니다.** 확인했습니다.
  회전은 문틀을 완전히 지난 뒤에.
- 복도에 사람이 다닙니다. 로봇을 세워둔 채 자리를 뜨지 마세요.
- 배터리 50% 이상. 조종 장치는 손에.
  게임패드 **L2+B** 가 진짜 한 방 정지(Damping)입니다.
